# catkin build artifacts (repo lives at src/ level; kept for safety if initialized higher)
build/
devel/

# runtime outputs
super_lio/map/
super_lio/logs/
*.pcd
*.bag

# livox_ros_driver is installed separately (see README), the workspace links it in
livox_ros_driver

# local tooling / editor noise
.claude/
.vscode/
.idea/
__pycache__/
*.swp

# M-detector upstream README assets
M-detector/img/
